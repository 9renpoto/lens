# Generate unique values before deploying. Do not commit the resulting .env file.
POSTGRES_PASSWORD=change-this-postgres-password
SECRET_KEY_BASE=change-this-secret-key-base

# The API is published only on this host's loopback interface.
LENS_PORT=4000
PHX_HOST=localhost
